# 小组分工方案

> 小组人数：4 人  
> 确定日期：2026-05-31  
> 修订日期：2026-06-01（调整：预处理由 B 承担，A 转向数据分析与可视化）  
> 最终确认：2026-06-10（基于实际 Git 记录核定）

---

## 分工总览


| 成员                   | 角色             | 计划占比 | 最终占比    | 核心职责                                                        |
| -------------------- | -------------- | ---- | ------- | ----------------------------------------------------------- |
| **A** (goat110110)   | 数据分析 + 可视化     | 22%  | **22%** | 数据 EDA、词汇分析、CV 可视化、消融实验、模型对比图表                              |
| **B** (nika)         | 预处理 + 分类模型     | 28%  | **30%** | 文本清洗、DataLoader、BERT 分类器、CV、调参、Backbone 对比、阈值校准、Soft Voting |
| **C** (Yudong Zhang) | 可解释性           | 24%  | **25%** | LLM 客户端、Prompt 模板、注意力可视化、端到端推理、解释方式对比、忠实度评估                 |
| **D** (chartte02/组长) | 项目集成 + 文档 + 报告 | 26%  | **23%** | 环境搭建、初始框架、项目重构、README、AGENTS、Report                         |


---

## 最终贡献评估（基于 Git 提交记录的实际分析）

> 评估方法：结合 Git 提交次数、代码行数（含初始框架代码的复用比例）、技术复杂度、与课程评分标准的对应关系、是否超出原计划交付，四个维度综合评定。

### 量化数据总览


| 指标         | B (nika) | C (Yudong Zhang) | D (chartte02)   | A (goat110110) |
| ---------- | -------- | ---------------- | --------------- | -------------- |
| Git 提交次数   | 12       | 5                | 4               | 6              |
| 代码净增行数     | ~2,165   | ~1,245           | ~1,683          | ~591           |
| 当前模块归属（估算） | ~1,060 行 | ~1,284 行         | ~2,070 行 + 全部文档 | ~537 行         |
| 核心交付文件数    | 15+      | 8                | 16+             | 8              |


> **说明**：D 的 Git 行数包含 train.csv / val.csv 等数据文件（~3,200 行），扣除后实际代码约 1,683 行，其中约 1,400 行为初始框架代码（后续由 B 大幅改造），但作为项目基础具有不可替代的脚手架价值。所有文档（README、AGENTS、report.md、division.md、ai_pipeline.md 等合计 ~1,060 行）均由 D 撰写。

---

### 成员 B (nika) — 预处理 + 分类模型：**30%**（→ 上调 +2%）

**实际交付 vs 计划：超出预期**


| 原计划                | 实际交付                                                                       | 状态    |
| ------------------ | -------------------------------------------------------------------------- | ----- |
| 预处理 pipeline       | `model/preprocessing.py` 最终版（5 步清洗）                                        | ✅     |
| DataLoader + CV 划分 | `model/data.py`（RumorDataset + `build_fold_datasets` + `stratified_split`） | ✅     |
| BERT 分类器           | `model/model.py`（dtype 修复、eager attention 支持）                              | ✅     |
| 训练循环               | `model/trainer.py` 大幅改造（类别加权、梯度裁剪、早停、阈值校准、tqdm 进度条、`_compute_metrics` 提取）  | ✅ 超出  |
| 交叉验证               | `run_cross_validation()` 完整实现，每折按 macro_f1 选最佳 epoch                       | ✅     |
| 训练/评估脚本            | `scripts/train.py` / `scripts/eval.py` 修改适配新 trainer                       | ✅     |
| —                  | `**scripts/calibrate.py`**（阈值扫描 τ∈[0.05,0.95]）                             | 🆕 额外 |
| —                  | `**scripts/compare_backbones.py**`（5 模型对比评估）                               | 🆕 额外 |
| —                  | `**scripts/vote.py**`（8 模型 soft voting）                                    | 🆕 额外 |
| —                  | 远程服务器训练 5 个 Backbone（twitter-roberta / DeBERTa-v3 ×2 / BERTweet / RoBERTa） | 🆕 额外 |
| —                  | 模型权重 + tokenizer 文件管理                                                      | 🆕 额外 |


**评估**：B 是项目中提交次数最多（12 次）、技术复杂度最高、代码改动最深入的人。不仅完成了原计划的全部任务，还额外交付了阈值校准、Backbone 对比、Soft Voting 三个评估工具，以及远程服务器上的 5 模型对比训练实验。`trainer.py` 从最初的 345 行框架代码被深度改造为 537 行生产级训练模块，新增的类别加权、早停、阈值校准直接贡献了 val 准确率从 ~84% 到 88.03% 的提升。**上调 +2%（28% → 30%）**。

---

### 成员 C (Yudong Zhang) — 可解释性模块：**25%**（→ 上调 +1%）

**实际交付 vs 计划：完全达成且质量高**


| 原计划        | 实际交付                                                        | 状态    |
| ---------- | ----------------------------------------------------------- | ----- |
| LLM API 封装 | `model/llm_client.py`（OpenAI 兼容、指数退避重试、4xx/5xx 区分处理）        | ✅     |
| Prompt 模板  | `model/prompts.py`（system prompt + 3 种场景模板）                 | ✅     |
| 注意力可视化     | `model/attention_viz.py`（提取/聚合/关键词排序/高亮 Markdown/热力图）       | ✅     |
| 端到端推理      | `scripts/predict.py`（大幅扩展至 303 行，支持 LLM 解释 + 注意力 + 热力图三种模式） | ✅ 超出  |
| 解释质量评估     | `scripts/eval_explain.py`（抽样 + LLM + CSV 输出 + 注意力关键词）       | ✅     |
| —          | `scripts/diagnose.py`（环境诊断脚本）                               | 🆕 额外 |
| —          | `model/model.py` 添加 `return_attentions` 支持                  | 🆕 额外 |
| —          | `**scripts/compare_explain_modes.py`**（三种解释方式说服力对比）         | 🆕 额外 |
| —          | `**scripts/compare_llms.py**`（多 LLM 模型横向对比）                 | 🆕 额外 |
| —          | `**scripts/eval_faithfulness.py**`（解释忠实度评估）                 | 🆕 额外 |


**评估**：C 的可解释性模块代码质量高、设计清晰、文档齐全。LLM 客户端封装完善（重试/错误处理/多模型支持），注意力可视化模块技术实现精细（子词合并、标点过滤、三种输出形式）。`predict.py` 的端到端集成是评分标准中"输入文本 → 输出标签+解释"要求的关键实现。在 `zcnew` 分支上追加了三种解释方式对比实验、多 LLM 模型横向对比和忠实度评估三个评估工具（共 925 行代码），进一步丰富了可解释性方案的评估维度。**上调 +1%（24% → 25%）**。

---

### 成员 D (chartte02/组长) — 项目集成 + 文档 + 报告：**23%**（→ 下调 -3%）

**实际交付 vs 计划：文档产出超出，让渡占比给 B/A**


| 原计划         | 实际交付                                                           | 状态    |
| ----------- | -------------------------------------------------------------- | ----- |
| 项目环境 + 目录结构 | `.venv`、`requirements.txt`、`model/` `scripts/` `outputs/` 目录体系 | ✅     |
| 初始代码框架      | 全部 11 个 `.py` 文件初始版本（~1,400 行）                                 | ✅     |
| 项目重构        | `ai_*` → 去前缀，全仓库 7 文件引用更新                                      | 🆕 额外 |
| README.md   | 完整定稿（含实际实验结果、运行命令、技术方案）                                        | ✅     |
| report.md   | 完整初稿（含消融实验、端到端示例、6 个问题详细分析）                                    | ✅     |
| —           | `AGENTS.md`（~200 行项目知识库）                                       | 🆕 额外 |
| —           | `doc/division.md`（分工方案 + 基于实际 Git 记录的贡献评估 + 占比调整）              | 🆕 额外 |
| —           | `doc/ai_pipeline.md`（AI 框架链路总结）                                | 🆕 额外 |
| —           | `scripts/generate_report_figures.py`（5 张报告图表生成）                | 🆕 额外 |
| —           | 报告 §3 的 6 个问题详细技术分析                                            | 🆕 额外 |
| —           | A 未交付的 CV 图表、混淆矩阵、消融实验初版由 D 生成                                 | 🆕 额外 |
| —           | 报告 §2.3 节（除消融实验外）由 D 撰写                                        | 🆕 额外 |


**评估**：D 完成了项目启动、框架搭建、重构集成和全部 5 份文档的撰写。初始框架代码为团队提供了可运行的起点，全部文档由 D 独力完成。额外交付了项目重构、报告图表生成脚本、6 个问题的详细技术分析、贡献评估分析。但核心 ML 算法实现由 B 承担、可解释性由 C 承担，D 的贡献偏向"集成与文档"而非算法创新，A 追加后 D 让渡 3% 给 A 和 B。**下调 -3%（26% → 23%）**。

---

### 成员 A (goat110110) — 数据分析 + 可视化：**22%**（→ 持平，恢复至计划值）

**实际交付 vs 计划：截止前追加补交，缺口基本补齐**


| 原计划        | 实际交付                                                                 | 状态       |
| ---------- | -------------------------------------------------------------------- | -------- |
| 数据 EDA     | `src/eda.py`（标签分布 / 文本长度 / 事件分布分析 + 6 张图表）                           | ✅        |
| 词汇与语言特征分析  | `src/vocabulary_analysis.py`（关键词对比 / 词云 / 特殊字符分析）                    | ✅        |
| 报告图表素材     | `outputs/figures/` 6 张 + `outputs/tables/` 6 个 CSV                   | ✅        |
| —          | `**src/analyze_cv_results.py`**（CV 结果分析脚本，122 行）                     | 🆕 追加    |
| —          | `**src/generate_ablation_report.py**`（消融实验脚本，198 行）                  | 🆕 追加    |
| —          | `**src/generate_evaluation_figures.py**`（评估图表批量生成，162 行）             | 🆕 追加    |
| —          | `**outputs/cv_accuracy.png` / `cv_macro_f1.png` / `cv_boxplot.png**` | 🆕 追加    |
| —          | `**outputs/performance_by_event.png**`（按事件性能图）                       | 🆕 追加    |
| —          | `**outputs/ablation_study.png**` + `ablation_report_output.txt`      | 🆕 追加    |
| —          | `**outputs/model_comparison.png**`（多模型综合对比）                          | 🆕 追加    |
| 交叉验证结果可视化  | 已通过新脚本补上                                                             | ✅ 补交     |
| 混淆矩阵       | 已生成                                                                  | ✅ 补交     |
| 消融实验       | 已通过新脚本补上                                                             | ✅ 补交     |
| 错误案例分析     | —                                                                    | ❌ 仍未交付   |
| 撰写报告 2.3 节 | —                                                                    | ❌ 由 D 代写 |


**评估**：A 在截止前追加了 4 次提交（`dcbaa27`/`ab23eab`/`4344a5c`/`17ac835`），补交了 CV 可视化、混淆矩阵和消融实验三个原计划缺失的重要产出。新增 3 个分析脚本（482 行代码）和 6 张图表。消融实验结果（预处理 Acc +0.75%、Precision +0.078）为报告 §2.3 提供了关键论据。考虑到缺口已基本补齐且额外交付了消融实验分析，**恢复至原计划的 22%（20% → 22%）**。错误分析和报告 2.3 节撰写仍由 D 补位，不再追加。

---

## 占比调整依据汇总


| 成员    | 原计划 | 最终      | 变动  | 核心理由                                              |
| ----- | --- | ------- | --- | ------------------------------------------------- |
| **B** | 28% | **30%** | +2% | 超额交付校准/对比/Voting 三件套 + 5 Backbone 训练实验 + 训练模块深度改造 |
| **C** | 24% | **25%** | +1% | 注意力可视化超出"仅LLM"计划 + predict.py 深度集成 + 解释方式对比/忠实度评估 |
| **D** | 26% | **23%** | -3% | 文档和框架搭建扎实，让渡占比给贡献更突出的 B/A                         |
| **A** | 22% | **22%** | 持平  | 首轮 EDA 完成但近半任务未做；截止前补交 CV 图 + 消融实验 + 模型对比 → 恢复计划值 |


---

## 分工与评分标准对应（最终版）


| 评分项           | 分值  | 实际贡献者                                                                                         |
| ------------- | --- | --------------------------------------------------------------------------------------------- |
| 大作业报告叙述清楚     | 30  | **D**（主笔：report.md + README + AGENTS）+ **A**（EDA 图表 6 张 + CV 可视化 4 张 + 消融实验图表）+ **C**（可解释性素材） |
| 代码可运行、部署说明清楚  | 25  | **D**（项目框架 + README + 环境搭建 + 重构）+ **B**（核心模型代码 + 训练链路）                                        |
| 分类准确率（88.03%） | 15  | **B**（核心贡献：类别加权 + 梯度裁剪 + 早停 + 阈值校准 + Backbone 选型）+ **A**（消融实验验证预处理有效性）                        |
| 检测依据的可解释性     | 15  | **C**（核心贡献：LLM 客户端 + Prompt 模板 + 注意力可视化 + 解释方式对比 + 忠实度评估）                                     |
| 小组分工协作情况      | 15  | 全员（Git 提交涵盖 4 人，PR review 记录完整，分支开发合并规范）                                                      |


---

## 协作规范

1. **代码提交**：每位成员在自己的功能分支上开发，通过 PR 合并到 master
2. **Commit 规范**：使用中文描述，格式 `模块: 做了什么`
3. **沟通频率**：每周至少一次同步进度，组长负责组织
4. **代码风格**：所有注释和文档使用中文，变量/函数名使用英文
5. **可复现性**：所有随机种子固定为 42，超参记录在配置文件中

